package ReplikaGenerateKey.repository;

import ReplikaGenerateKey.model.Keys;
import org.springframework.data.jpa.repository.JpaRepository;

public interface KeysRepository extends JpaRepository<Keys, Long> { }
